# MkDocs Projects Server - Environment Configuration
# ===================================================
# ALLE Variablen MÜSSEN gesetzt werden. Es gibt keine Default-Werte.
# Kopiere diese Datei nach .env und passe die Werte an.

# User & Group IDs (mapped from host to container)
# Ermitteln mit: id -u && id -g
USER_ID=
GROUP_ID=

# Host-Pfade (Bind-Mounts) - ABSOLUTE Pfade erforderlich!
# Empfohlen: /srv/appdata/mkdocs/projects und /srv/appdata/mkdocs/site
PROJECTS_DIR=
SITE_DIR=

# Nginx-Port
NGINX_PORT=

# Container-Namen
CONTAINER_BUILDER=
CONTAINER_NGINX=

# Hinweise:
# --------
# 1. Alle Variablen müssen gesetzt sein, sonst startet der Stack nicht
#
# 2. Ordner müssen existieren und dem User gehören:
#    mkdir -p /srv/appdata/mkdocs/projects /srv/appdata/mkdocs/site
#    chown -R $(id -u):$(id -g) /srv/appdata/mkdocs
#
# 3. USER_ID und GROUP_ID ermitteln:
#    id -u  →  USER_ID
#    id -g  →  GROUP_ID
#
# 4. Nach Änderungen:
#    docker compose down
#    docker compose up -d --build
