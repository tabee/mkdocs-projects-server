# MkDocs Projects Server - Environment Configuration

# User & Group IDs (mapped from host to container)
# Auto-detect: USER_ID=$(id -u) && GROUP_ID=$(id -g)
USER_ID=1000
GROUP_ID=1000

# Host-Pfade (Bind-Mounts)
# Alle Daten liegen lokal auf dem Host unter deiner Kontrolle
PROJECTS_DIR=/home/username/docs-projects
SITE_DIR=/home/username/docs-site

# Nginx-Port (nur localhost)
NGINX_PORT=8080

# Container-Namen (optional, aber sauberer)
CONTAINER_BUILDER=mkdocs-builder
CONTAINER_NGINX=docs-nginx

# Hinweise:
# --------
# 1. Setze PROJECTS_DIR und SITE_DIR auf absolute Pfade
#    (z.B. /home/username/... oder /root/... je nach deinem User)
#
# 2. Die Ordner müssen existieren und dir gehören:
#    mkdir -p ~/docs-projects ~/docs-site
#
# 3. USER_ID und GROUP_ID sollten deine realen IDs sein:
#    id -u       → USER_ID
#    id -g       → GROUP_ID
#
# 4. Nach Änderungen:
#    docker compose down
#    docker compose up -d --build
